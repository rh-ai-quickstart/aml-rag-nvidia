# Default values for bootstrap chart
# This chart creates the bootstrap Application (app-of-apps parent)

bootstrap:
  # Git repository URL
  repoURL: https://github.com/rh-aiservices-bu/rag-blueprint-nvidia
  # Branch or tag to use
  targetRevision: restructure-apps

ingest:
  nvidiaApiKey:
    create: false
